// asdf
How it work:
if <file_chnages> {
  git checkout -b <branch>
  git add .
  git commit -m <comment>
}

if <files_to_pull> {
  git pull --no-ff --no-commit origin <base_branch>  
  rm -fr ".git/rebase-merge"
}

if <not_conflicts> {  
  git checkout <base_branch>
  git merge <branch_name>
  git push -f origin <base_branch>
}
